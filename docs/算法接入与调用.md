# 算法接入与调用

> **文档版本**：v1.8  
> **更新日期**：2026-08-03  
> **仓库**：`IPC_Station2`（第二工位 IPC）  
> **读者**：IPC 开发 / 算法对接 / 接手 AI Agent

---

## 1. 文档目的

本文档记录第二工位各测量算法的 **工程接入状态** 与 **调用方式**。

接入原则：

- **动态库（DLL）方式引入**，不将算法源码编入 IPC 工程。
- 算法仓独立编译 → 打包 SDK → 工位仓 `third_party/` 引用 → `modules/` 薄封装 → 主流程调用。
- 采集（Mech-Eye / 海康）与算法解耦；算法只吃内存中的点云或约定格式的文件。

主流程（PLC 触发、阈值判定、HMI 展示）的联调节奏见 [flow_control 架构说明](./guides/flow_control架构说明.md)；本文档侧重 **算法层是否就绪、如何调用**。

---

## 2. 算法总览

| 序号 | 算法 | 源码目录（算法仓） | IPC 模块 | SDK 目录 | 接入状态 |
|------|------|-------------------|----------|----------|----------|
| 1 | **焊缝截面测量** | `第二工位测量源码/V2.0/焊缝测量-VS2022-V2.0` | `modules/weld_measure` | `third_party/weld_measure` | **已接入 V2.0 SDK（Inspection 暂短路）** |
| 2 | **厚度测量** | `第二工位测量源码/厚度测量` | `modules/thickness_measure` | `third_party/thickness_measure` | **已接入（path4 已调用）** |
| 3 | **内表面测量** | `第二工位测量源码/内表面测量` | `modules/inner_surface_measure` | `third_party/inner_surface_measure` | **已接入（path4 已调用）** |
| 4 | **咬边长度测量** | `第二工位测量源码/咬边长度测量-VS2022` | `modules/undercut_length_measure` | `third_party/undercut_length_measure` | **已接入（主流程未调用）** |
| 5 | **长度和容积测量** | `第二工位测量源码/V2.0/长度和容积测量-V2.0` | `modules/length_volume_measure` | `third_party/length_volume_measure` | **已接入 V2.0 第二版 SDK（path3）** |
| 6 | **吊装点云碰撞检测** | `第二工位测量源码/辅助吊装中的碰撞检测-V1.2` | `modules/collision_monitor`（待建） | `third_party/collision_detection` | **SDK 已打包；薄封装/主流程未接** |

算法仓说明见 `第二工位测量源码/说明.txt`。

---

## 3. 通用接入模式（后续算法参照）

后续算法建议复用与 **焊缝测量** 相同的模式：

```text
算法 VS 工程
  └─ 新增 DynamicLibrary 工程 + 稳定 C API 头文件
  └─ scripts/package_sdk_to_ipc.ps1  →  third_party/<name>/

IPC_Station2
  ├─ cmake/<Name>Sdk.cmake          # IMPORTED 目标 + POST_BUILD 部署
  ├─ modules/<name>/                # Qt/C++ 薄封装（Service / Types）
  ├─ app/CMakeLists.txt             # scan_tracking_deploy_*_runtime
  └─ flow_control / 其它模块        # 业务调用点（可后接）
```

**SDK 目录约定**（以焊缝为例）：

```text
third_party/weld_measure/
├── include/weld_measure_c_api.h
├── lib/Release/WeldMeasure.lib
├── lib/Debug/WeldMeasure.lib          # 可选；缺失时 Debug 复用 Release
├── bin/Release/WeldMeasure.dll
├── bin/Release/onnxruntime.dll
├── models/pointnet_weld_seam_V7.3_good.onnx
└── weld_measurement.ini               # 参考配置（运行时非必须）
```

**运行时部署**（相对 `scan-tracking.exe`）：

| 文件 | 路径 |
|------|------|
| 算法 DLL | 与 exe 同目录 |
| ONNX Runtime（焊缝） | 与 exe 同目录 |
| PCL 运行时（厚度 / 内表面 / 长度容积） | 与 exe 同目录 |
| 焊缝模型 | `models/weld_measure/*.onnx` |
| 厚度参考配置 | `config/thickness_measure/thickness_config.json` |
| 内表面参考配置 | `config/inner_surface_measure/config.ini` |
| 咬边长度参考配置 | `config/undercut_length_measure/undercut_length.ini` |
| 长度容积参考配置 | `config/length_volume_measure/config.ini` |

---

## 4. 焊缝截面测量（已接入 V2.0）

### 4.1 功能说明

单截面焊缝几何测量，流程：

1. PointNet ONNX 识别左右焊趾  
2. 错边量、余高、母材夹角、棱角度  
3. 左右咬边深度（单截面）

> **源码**：`第二工位测量源码/V2.0/焊缝测量-VS2022-V2.0/焊缝测量-VS2022`（V2.0 内核 + v142 DLL 壳）  
> **状态**：✅ SDK + 薄封装已接入（V2.0）；⏸ Inspection 入口仍有 `TODO(algo)` 短路，运行时暂不调 DLL  
> **不包含**：多截面咬边**长度**统计（见第 7 节「咬边长度测量」）。

### 4.2 接入状态

| 项 | 状态 |
|----|------|
| 算法 DLL `WeldMeasure.dll` | ✅ 已编译 V2.0（v142 / x64 / `/MD`） |
| C API `weld_measure_c_api.h` | ✅（与 V1 兼容） |
| SDK 打包至 `third_party/weld_measure` | ✅ |
| CMake `WeldMeasureSdk.cmake` | ✅ |
| 封装模块 `scan_tracking_weld_measure` | ✅（无需改代码） |
| 链接进 `scan-tracking` + 运行时拷贝 | ✅ |
| Debug 工程编译通过 | ✅（Debug 暂用 Release DLL） |
| 样例截面冒烟测试 | ✅ |
| 主流程 PLC/HMI 完整联调 | ⏸ 待后续（Inspection 暂短路） |
| 输入截面切片（整段云 → 截面） | ⏸ 待后续 |

### 4.3 工程路径

| 用途 | 路径 |
|------|------|
| 算法源码 / DLL 工程 | `D:\work\LY\第二工位测量源码\V2.0\焊缝测量-VS2022-V2.0\焊缝测量-VS2022` |
| DLL 工程 | `WeldMeasureDll.vcxproj` |
| 打包脚本 | `V2.0/焊缝测量-VS2022-V2.0/焊缝测量-VS2022/scripts/package_sdk_to_ipc.ps1` |
| IPC SDK | `third_party/weld_measure/` |
| CMake | `cmake/WeldMeasureSdk.cmake` |
| 封装头文件 | `modules/weld_measure/include/scan_tracking/weld_measure/` |
| 封装实现 | `modules/weld_measure/src/weld_measure_service.cpp` |

### 4.4 依赖

| 依赖 | 说明 |
|------|------|
| ONNX Runtime 1.18.1 | 随 SDK 部署 `onnxruntime.dll` |
| Eigen 3.x | **仅 DLL 内部**使用；工位工程不依赖 |
| PCL | **已去除**；点云为轻量 POD 结构 |

工具链：算法 DLL 与 IPC 均使用 **MSVC v142（14.29）x64**。

### 4.5 输入 / 输出

**输入**

- 类型：`float` 数组，**交错** `x,y,z,x,y,z,...`
- 语义：**单个焊缝截面**点云（单位 mm）
- 非有限值（NaN/Inf）在 DLL 内自动跳过

**输出**（`WeldSectionMeasurement` / `wm_section_result`）

| 字段 | 含义 |
|------|------|
| `mismatchMm` | 错边量 (mm) |
| `reinforcementMm` | 余高 (mm) |
| `angularityMm` | 棱角度 (mm) |
| `includedAngleRad` | 母材延长线夹角 (rad) |
| `leftUndercutMm` / `rightUndercutMm` | 左右咬边深度 (mm) |
| `maxUndercutMm` | 截面最大咬边深度 (mm) |
| `leftToe*` / `rightToe*` | 左右焊趾坐标 |
| `valid` | 测量是否有效 |

### 4.6 调用方式

#### 方式 A：C++ 封装（推荐）

```cpp
#include "scan_tracking/weld_measure/weld_measure_service.h"

using scan_tracking::weld_measure::WeldMeasureService;
using scan_tracking::weld_measure::WeldSectionMeasurement;
using scan_tracking::weld_measure::WeldMeasureError;

WeldMeasureService service;
WeldMeasureError err;
if (!service.initialize(/* modelPath */ QString(), &err)) {
    // err.message
    return;
}

WeldSectionMeasurement result;
if (!service.measureSection(xyz, pointCount, &result, &err)) {
    // err.statusCode, err.message
    return;
}
// result.mismatchMm, result.reinforcementMm, ...
```

- 默认模型路径：`<applicationDir>/models/weld_measure/pointnet_weld_seam_V7.3_good.onnx`
- 同一 `WeldMeasureService` 实例内调用已串行化（DLL 内部 mutex）
- 使用前需 `initialize()`；进程退出或切换模型前可 `shutdown()`

#### 方式 B：C API（跨模块 / 工具）

头文件：`third_party/weld_measure/include/weld_measure_c_api.h`

```c
wm_onnx_config onnx;
wm_onnx_config_default(&onnx);
onnx.model_path = "D:/path/to/model.onnx";

wm_context* ctx = NULL;
wm_create(&onnx, &ctx);

wm_options opt;
wm_options_default(&opt);

wm_section_result result;
char msg[512];
wm_measure_section(ctx, xyz, point_count, &opt, &result, msg, sizeof(msg));

wm_destroy(ctx);
```

### 4.7 与 flow_control 的关系

`Trig_Inspection` → `evaluateStation2Inspection()` 已按路径 `algorithm` 路由：

| pathId | name | algorithm | 行为 |
|--------|------|-----------|------|
| 1 | `straight_weld` | `weld_section` | 配额齐后对臂+伸缩杆各段调用 `WeldMeasureService` |
| 5 | `ring_weld` | `weld_section` | 同上（配额 18+18） |
| 3 | `length_volume` | `length_volume` | 臂×7 外表面云合并后长度+容积（与现场一致） |
| 4 | `thickness_inner_surface` | `thickness_inner_surface` | 臂×12 厚度配对 + 内表面两端平均（与现场一致） |
| 2 | `code_read` | `code_read` | `Trig_CodeRead` / `Trig_Inspection` → 海康 C NumberRecognition OCR |

切换路径：改 `activePathId` 后重启；建议先 `Trig_ResultReset` 清缓存。

仍待生产确认：

- 截面切片策略（当前直接喂整段点云）
- 多段聚合规则与 NG 阈值
- PLC `NG_Reason_Word*` 细项映射
- 厚度/内表面模板 PCD 现场路径

**无需再改 SDK 接入层**（新算法继续加路由分支即可）。

### 4.8 更新算法版本

在算法仓 `V2.0/焊缝测量-VS2022-V2.0/焊缝测量-VS2022` 执行：

```powershell
# 1. 编译 DLL
msbuild WeldMeasureDll.vcxproj /p:Configuration=Release /p:Platform=x64

# 2. 打包到 IPC third_party
powershell -ExecutionPolicy Bypass -File .\scripts\package_sdk_to_ipc.ps1 -Configuration Release
```

然后在 IPC 仓重新编译 `scan-tracking` 即可（C API 兼容时薄封装无需改）。

### 4.9 已知限制

1. **输入必须是截面点云**；Mech-Eye 整段扫描云需后续增加切片或离线截面文件。  
2. **咬边长度**不在本 DLL 内，需接入「咬边长度测量」算法。  
3. Debug 配置若未单独打包 Debug DLL，CMake 会回退使用 Release 版。  
4. ONNX 模型路径建议使用绝对路径或相对 exe 的部署路径，避免依赖进程 cwd。  
5. V2.0 控制台 exe 另含 ICP/截面切片/多帧咬边长度；**当前 DLL 仍暴露单截面 `wm_measure_section`**（与 IPC 现有封装一致）。咬边默认参数已对齐 V2.0（`toe_search_width=5`、`base_fit` 5~40 mm）。

---

## 5. 厚度测量（已接入，path4 已调用）

> **源码**：`第二工位测量源码/厚度测量`  
> **状态**：✅ SDK + 薄封装已接入；✅ `flow_control` path4 已调用

### 5.1 功能说明

- **输入**：多对连续帧点云（内表面一帧 + 外表面一帧为一对）  
- **输出**：筒体焊缝附近厚度；多对时可输出 **平均厚度**

流程概要：内外扫描点云预处理 → 分别与内外模板 ICP → 在模板坐标系下按特征点求厚度（默认 `nearest_between_surfaces`）。

### 5.2 接入状态

| 项 | 状态 |
|----|------|
| 算法 DLL `ThicknessMeasure.dll` | ✅ 已编译（v142 / x64 / `/MD`，Release） |
| C API `thickness_measure_c_api.h` | ✅ |
| SDK 打包至 `third_party/thickness_measure` | ✅（当前仅 Release；Debug 回退 Release） |
| CMake `ThicknessMeasureSdk.cmake` | ✅ |
| 封装模块 `scan_tracking_thickness_measure` | ✅ |
| 链接进 `scan-tracking` + 运行时拷贝 | ✅ |
| 主流程 PLC/HMI 调用 | ✅ path4 / `Trig_Inspection` |
| 模板点云现场部署路径 | ⏸ 待现场配置（`initializeFromJson` 依赖 JSON 内模板路径） |

### 5.3 工程路径

| 用途 | 路径 |
|------|------|
| 算法源码 / DLL 工程 | `D:\work\LY\第二工位测量源码\厚度测量` |
| DLL 工程 | `ThicknessMeasureDll.vcxproj` |
| 打包脚本 | `厚度测量/scripts/package_sdk_to_ipc.ps1` |
| IPC SDK | `third_party/thickness_measure/` |
| CMake | `cmake/ThicknessMeasureSdk.cmake` |
| 封装头文件 | `modules/thickness_measure/include/scan_tracking/thickness_measure/` |
| 封装实现 | `modules/thickness_measure/src/thickness_measure_service.cpp` |

### 5.4 依赖

| 依赖 | 说明 |
|------|------|
| PCL 1.12（vc142） | **仅 DLL 内部**；运行时随 SDK 部署 `pcl_*.dll` |
| Eigen / Boost / FLANN | **仅 DLL 内部**；工位工程不依赖 |
| 轻量 PCD 加载 | DLL 内 `SimplePcdIo`，不依赖 `pcl_io`/VTK/OpenNI |

工具链：算法 DLL 与 IPC 均使用 **MSVC v142（14.29）x64**。IPC Debug 工程可链接 **Release** 算法库（与焊缝同策略）。

### 5.5 输入 / 输出

**输入**

- 扫描点云：`float` 交错 `x,y,z,...`（单位 mm）；非有限值在 DLL 内跳过  
- 一对 = 内表面扫描 + 外表面扫描  
- 模板：内存点云，或由 `thickness_config.json` 中的 PCD 路径加载

**输出**

| 类型 | 主要字段 |
|------|----------|
| 单对 `ThicknessPairMeasurement` | `thicknessMm`、内外 ICP fitness、最近点等 |
| 多对平均 `ThicknessAverageMeasurement` | `thicknessMm`（成功对均值）、`successCount` |

### 5.6 调用方式（供后续主流程使用；当前未接线）

#### 方式 A：C++ 封装（推荐）

```cpp
#include "scan_tracking/thickness_measure/thickness_measure_service.h"

using scan_tracking::thickness_measure::ThicknessMeasureService;
using scan_tracking::thickness_measure::ThicknessPairMeasurement;
using scan_tracking::thickness_measure::ThicknessMeasureError;

ThicknessMeasureService service;
ThicknessMeasureError err;
if (!service.initializeFromJson(/* configPath */ QString(), &err)) {
    // 或 service.initialize(config, innerTpl, nInner, outerTpl, nOuter, &err);
    return;
}

ThicknessPairMeasurement result;
if (!service.measurePair(innerXyz, innerCount, outerXyz, outerCount, &result, &err)) {
    return;
}
// result.thicknessMm
```

- 默认配置路径：`<applicationDir>/config/thickness_measure/thickness_config.json`
- 同一实例内调用已串行化
- 多对平均：`measurePairsAverage(...)`

#### 方式 B：C API

头文件：`third_party/thickness_measure/include/thickness_measure_c_api.h`

```c
tm_context* ctx = NULL;
tm_create_from_json("D:/path/thickness_config.json", &ctx);

tm_pair_result result;
char msg[512];
tm_measure_pair(ctx, inner_xyz, inner_n, outer_xyz, outer_n, &result, msg, sizeof(msg));

tm_destroy(ctx);
```

### 5.7 与 flow_control 的关系

`Trig_Inspection` 在 `algorithm=thickness_inner_surface`（path4）时调用 `ThicknessMeasureService`：

- 点位 `purpose=thickness_inner` + `thickness_outer` 按序配对（当前 1↔2、7↔8）
- 多对调用 `measurePairsAverage`，结果写入 `headMetrics.thicknessMm`
- 初始化：`config/thickness_measure/thickness_config.json`（随 SDK 部署到 exe 旁）
- **模板 PCD 路径须现场配置**；路径无效时 Res=2 并在 message 中提示

### 5.8 更新算法版本

在算法仓执行：

```powershell
msbuild ThicknessMeasureDll.vcxproj /p:Configuration=Release /p:Platform=x64
powershell -ExecutionPolicy Bypass -File .\scripts\package_sdk_to_ipc.ps1 -Configuration Release
```

然后在 IPC 仓重新编译 `scan-tracking` 即可。

### 5.9 已知限制

1. 当前 SDK **仅打包 Release**；Debug 配置下 CMake 回退使用 Release DLL（与焊缝一致）。  
2. `initializeFromJson` 依赖 JSON 内模板 PCD 的有效路径；生产环境建议改用内存模板 `initialize(...)`。  
3. 参考配置中的相对模板路径（如 `../input/...`）部署到 exe 旁后通常无效，需现场配置或改内存入口。  
4. ~~主流程尚未接线~~ → **R3 已接线**（path4 / `thickness_inner_surface`）。

---

## 6. 内表面测量（已接入，主流程已调用 path4）

> **源码**：`第二工位测量源码/内表面测量/InnerSurfaceMeasure`  
> **状态**：✅ SDK + 薄封装已接入；✅ `flow_control` path4 已调用

### 6.1 功能说明

- **输入**：筒体 **两端**点云（各一端一帧）  
- **输出**：内径（直径）、周长、圆度；两端测量后取 **平均**

流程概要：每帧体素下采样 + 统计离群 → ICP 对齐模板 → 圆柱拟合 → 轴向 25%/50%/75% 截面圆度 → 两端结果平均。

与伸缩杆内部扫描时序关系见 [2.4 圆筒伸缩杆内部扫描时序图](./sequence_diagrams/2.4_圆筒伸缩杆内部扫描时序图.md)。

### 6.2 接入状态

| 项 | 状态 |
|----|------|
| 算法 DLL `InnerSurfaceMeasure.dll` | ✅ 已编译（v142 / x64 / `/MD`，Release） |
| C API `inner_surface_measure_c_api.h` | ✅ |
| SDK 打包至 `third_party/inner_surface_measure` | ✅（当前仅 Release；Debug 回退 Release） |
| CMake `InnerSurfaceMeasureSdk.cmake` | ✅ |
| 封装模块 `scan_tracking_inner_surface_measure` | ✅ |
| 链接进 `scan-tracking` + 运行时拷贝 | ✅ |
| 主流程 PLC/HMI 调用 | ✅ path4 / `Trig_Inspection` |
| 模板点云现场部署路径 | ⏸ 待现场配置（`initializeFromIni` 依赖 INI 内模板路径） |

### 6.3 工程路径

| 用途 | 路径 |
|------|------|
| 算法源码 / DLL 工程 | `D:\work\LY\第二工位测量源码\内表面测量\InnerSurfaceMeasure` |
| DLL 工程 | `InnerSurfaceMeasureDll.vcxproj` |
| 打包脚本 | `InnerSurfaceMeasure/scripts/package_sdk_to_ipc.ps1` |
| IPC SDK | `third_party/inner_surface_measure/` |
| CMake | `cmake/InnerSurfaceMeasureSdk.cmake` |
| 封装头文件 | `modules/inner_surface_measure/include/scan_tracking/inner_surface_measure/` |
| 封装实现 | `modules/inner_surface_measure/src/inner_surface_measure_service.cpp` |

### 6.4 依赖

| 依赖 | 说明 |
|------|------|
| PCL 1.12（vc142） | **仅 DLL 内部**；运行时随 SDK 部署 `pcl_*.dll` |
| Eigen / Boost / FLANN | **仅 DLL 内部**；工位工程不依赖 |
| 轻量 PCD 加载 | DLL 内 `SimplePcdIo`，不依赖 `pcl_io`/VTK/OpenNI |

工具链：算法 DLL 与 IPC 均使用 **MSVC v142（14.29）x64**。IPC Debug 工程可链接 **Release** 算法库（与焊缝/厚度同策略）。

### 6.5 输入 / 输出

**输入**

- 扫描点云：`float` 交错 `x,y,z,...`（单位 mm）；非有限值在 DLL 内跳过  
- 两端各一帧；模板：内存点云，或由 `config.ini` 中的 PCD 路径加载

**输出**

| 类型 | 主要字段 |
|------|----------|
| 单端 `InnerSurfaceFrameMeasurement` | `diameterMm`、`circumferenceMm`、三截面圆度、ICP fitness 等 |
| 两端平均 `InnerSurfaceAverageMeasurement` | `diameterMm`、`circumferenceMm`、`roundness` |

### 6.6 调用方式（供后续主流程使用；当前未接线）

#### 方式 A：C++ 封装（推荐）

```cpp
#include "scan_tracking/inner_surface_measure/inner_surface_measure_service.h"

using scan_tracking::inner_surface_measure::InnerSurfaceMeasureService;
using scan_tracking::inner_surface_measure::InnerSurfaceAverageMeasurement;
using scan_tracking::inner_surface_measure::InnerSurfaceMeasureError;

InnerSurfaceMeasureService service;
InnerSurfaceMeasureError err;
if (!service.initializeFromIni(/* configPath */ QString(), &err)) {
    // 或 service.initialize(config, templateXyz, n, &err);
    return;
}

InnerSurfaceAverageMeasurement result;
if (!service.measureTwoFramesAverage(xyz1, n1, xyz2, n2, &result, nullptr, nullptr, &err)) {
    return;
}
// result.diameterMm, result.circumferenceMm, result.roundness
```

- 默认配置路径：`<applicationDir>/config/inner_surface_measure/config.ini`
- 同一实例内调用已串行化
- 单端测量：`measureFrame(...)`

#### 方式 B：C API

头文件：`third_party/inner_surface_measure/include/inner_surface_measure_c_api.h`

```c
ism_context* ctx = NULL;
ism_create_from_ini("D:/path/config.ini", &ctx);

ism_average_result avg;
ism_frame_result f1, f2;
char msg[512];
ism_measure_two_frames_average(ctx, xyz1, n1, xyz2, n2, &avg, &f1, &f2, msg, sizeof(msg));

ism_destroy(ctx);
```

### 6.7 与 flow_control 的关系

`Trig_Inspection` 在 path4 时与厚度同路径调用 `InnerSurfaceMeasureService`：

- `purpose=inner_surface` 点位取两端（localIndex 最小/最大）一次 `ism_measure_two_frames_average`（与源码 exactly 2 帧一致；现场 path4 为点 3 与 12）
- 每对 `measureTwoFramesAverage`，再对成功对取平均 → `innerDiameterMm` / `innerCircumferenceMm` / `innerRoundness`
- 初始化：`config/inner_surface_measure/config.ini`（随 SDK 部署）
- **模板 PCD 路径须现场配置**

输出内径等指标后续可作为「长度和容积测量」输入。

### 6.8 更新算法版本

在算法仓执行：

```powershell
msbuild InnerSurfaceMeasureDll.vcxproj /p:Configuration=Release /p:Platform=x64
powershell -ExecutionPolicy Bypass -File .\scripts\package_sdk_to_ipc.ps1 -Configuration Release
```

然后在 IPC 仓重新编译 `scan-tracking` 即可。

### 6.9 已知限制

1. 当前 SDK **仅打包 Release**；Debug 配置下 CMake 回退使用 Release DLL（与焊缝/厚度一致）。  
2. `initializeFromIni` 依赖 INI 内模板 PCD 的有效路径；生产环境建议改用内存模板 `initialize(...)`。  
3. 参考配置中的相对模板路径（如 `./Data/...`）部署到 exe 旁后通常无效，需现场配置或改内存入口。  
4. ~~主流程尚未接线~~ → **R3 已接线**（path4）。

---

## 7. 咬边长度测量（已接入，主流程未调用）

> **源码**：`第二工位测量源码/咬边长度测量-VS2022`  
> **状态**：✅ SDK + 薄封装已接入；⏸ `flow_control` / 主流程 **尚未调用**

### 7.1 功能说明

- **输入**：焊缝测量输出的 **左右焊趾**、**左右咬边深度**（多截面序列）  
- **输出**：左右侧咬边 **长度**（最长连续超标区段 × 截面间隔）

判定概要：每截面 `depth >= depth_threshold_mm` → 中值滤波 → 最长连续咬边截面数 × `section_interval_mm`；左右侧独立计算。

### 7.2 接入状态

| 项 | 状态 |
|----|------|
| 算法 DLL `UndercutLengthMeasure.dll` | ✅ 已编译（v142 / x64 / `/MD`，Release） |
| C API `undercut_length_measure_c_api.h` | ✅ |
| SDK 打包至 `third_party/undercut_length_measure` | ✅（当前仅 Release；Debug 回退 Release） |
| CMake `UndercutLengthMeasureSdk.cmake` | ✅ |
| 封装模块 `scan_tracking_undercut_length_measure` | ✅ |
| 链接进 `scan-tracking` + 运行时拷贝 | ✅ |
| 主流程 PLC/HMI 调用 | ⏸ **未接**（按计划先接入后调用） |

### 7.3 工程路径

| 用途 | 路径 |
|------|------|
| 算法源码 / DLL 工程 | `D:\work\LY\第二工位测量源码\咬边长度测量-VS2022` |
| DLL 工程 | `UndercutLengthMeasureDll.vcxproj` |
| 打包脚本 | `咬边长度测量-VS2022/scripts/package_sdk_to_ipc.ps1` |
| IPC SDK | `third_party/undercut_length_measure/` |
| CMake | `cmake/UndercutLengthMeasureSdk.cmake` |
| 封装头文件 | `modules/undercut_length_measure/include/scan_tracking/undercut_length_measure/` |
| 封装实现 | `modules/undercut_length_measure/src/undercut_length_measure_service.cpp` |

### 7.4 依赖

| 依赖 | 说明 |
|------|------|
| 无第三方运行时 | 纯 C++ / `/MD`；无 ONNX / PCL |
| 上游数据 | **焊缝截面测量**（多截面焊趾 + 咬边深度） |

工具链：算法 DLL 与 IPC 均使用 **MSVC v142（14.29）x64**。IPC Debug 工程可链接 **Release** 算法库（与焊缝/厚度同策略）。

### 7.5 输入 / 输出

**输入**

- 多截面序列：每截面含左右焊趾 `(x,y,z)` + 左右咬边深度 (mm)  
- 可选：离线 `sections.txt`（8 或 9 列数字，见算法仓 README）

**输出**（`UndercutLengthMeasurement` / `ulm_result`）

| 字段 | 含义 |
|------|------|
| `left.lengthMm` / `right.lengthMm` | 左右咬边长度 (mm) |
| `left.maxDepthMm` / `right.maxDepthMm` | 左右最大咬边深度 (mm) |
| `left.maxContinuousSections` / `right.maxContinuousSections` | 最长连续咬边截面数 |
| `valid` | 测量是否有效 |

### 7.6 调用方式（供后续主流程使用；当前未接线）

#### 方式 A：C++ 封装（推荐）

```cpp
#include "scan_tracking/undercut_length_measure/undercut_length_measure_service.h"

using scan_tracking::undercut_length_measure::UndercutLengthMeasureService;
using scan_tracking::undercut_length_measure::UndercutSectionInput;
using scan_tracking::undercut_length_measure::UndercutLengthMeasurement;
using scan_tracking::undercut_length_measure::UndercutLengthMeasureError;

UndercutLengthMeasureService service;
UndercutLengthMeasureError err;
if (!service.initialize(/* options */ {}, &err)) {
    // 或 service.initializeFromIni(/* configPath */ QString(), &err);
    return;
}

QVector<UndercutSectionInput> sections;
// 由焊缝多截面结果填充 sections...
UndercutLengthMeasurement result;
if (!service.measure(sections, &result, &err)) {
    return;
}
// result.left.lengthMm, result.right.lengthMm
```

- 默认配置路径：`<applicationDir>/config/undercut_length_measure/undercut_length.ini`
- 生产环境优先 `initialize(options)` + 内存截面；INI 仅作参数参考
- 同一实例内调用已串行化

#### 方式 B：C API

头文件：`third_party/undercut_length_measure/include/undercut_length_measure_c_api.h`

```c
ulm_options opt;
ulm_options_default(&opt);

ulm_context* ctx = NULL;
ulm_create(&opt, &ctx);

ulm_result result;
char msg[512];
ulm_measure(ctx, sections, section_count, &result, msg, sizeof(msg));

ulm_destroy(ctx);
```

### 7.7 与 flow_control 的关系

**当前未在主流程中调用。** 模块已链接并部署运行时，后续接入时建议：

- 在 `station2_inspection.cpp` 中先完成多截面 `WeldMeasureService`，再将焊趾/咬边深度序列交给 `UndercutLengthMeasureService`
- 对齐 PLC / HMI 咬边长度字段与 NG 阈值

**无需再改 SDK 接入层。**

### 7.8 更新算法版本

在算法仓执行：

```powershell
msbuild UndercutLengthMeasureDll.vcxproj /p:Configuration=Release /p:Platform=x64
powershell -ExecutionPolicy Bypass -File .\scripts\package_sdk_to_ipc.ps1 -Configuration Release
```

然后在 IPC 仓重新编译 `scan-tracking` 即可。

### 7.9 已知限制

1. 当前 SDK **仅打包 Release**；Debug 配置下 CMake 回退使用 Release DLL。  
2. `initializeFromIni` 会解析 INI 中的 `[Input] sections_path`（算法侧 AppConfig 要求非空），但内存测量路径不使用该字段；生产建议 `initialize(options)`。  
3. 参考配置中的 `sections_path` 部署到 exe 旁后通常无效，仅作阈值参考。  
4. 主流程尚未接线，运行 `scan-tracking` 不会自动执行咬边长度测量。  
5. 与第 4 节焊缝 DLL 为 **上下游关系**：需先有多截面焊缝结果再聚合长度。

---

## 8. 长度和容积测量（已接入，主流程未调用）

> **源码**：`第二工位测量源码/V2.0/长度和容积测量-V2.0/长度和容积测量`（V2.0 第二版内核 + v142 DLL 壳）  
> **状态**：✅ SDK + 薄封装已接入（V2.0 第二版）；⏸ Inspection 入口仍有 `TODO(algo)` 短路，运行时暂不调 DLL

### 8.1 功能说明

- **输入**：筒体 **外表面**扫描点云 + 容积计算用半径（通常取内表面测量直径的一半）  
- **输出**：筒体 **长度 (mm)**、**容积 (L)**；另返回拟合外半径、端点位置、ICP 信息

流程概要：体素下采样 + 可选统计离群 → ICP 对齐模板 → 拟合圆柱轴/位置 → 粗/精定位两端 → 长度；`volumeLiters = π * volumeRadius² * length / 1e6`。

### 8.2 接入状态

| 项 | 状态 |
|----|------|
| 算法 DLL `LengthVolumeMeasure.dll` | ✅ 已编译（v142 / x64 / `/MD`，Release） |
| C API `length_volume_measure_c_api.h` | ✅ |
| SDK 打包至 `third_party/length_volume_measure` | ✅（当前仅 Release；Debug 回退 Release） |
| CMake `LengthVolumeMeasureSdk.cmake` | ✅ |
| 封装模块 `scan_tracking_length_volume_measure` | ✅ |
| 链接进 `scan-tracking` + 运行时拷贝 | ✅ |
| 主流程 PLC/HMI 调用 | ⏸ **未接**（按计划先接入后调用） |
| 模板点云现场部署路径 | ⏸ 待后续（`initializeFromIni` 依赖 INI 内模板路径） |

### 8.3 工程路径

| 用途 | 路径 |
|------|------|
| 算法源码 / DLL 工程 | `D:\work\LY\第二工位测量源码\V2.0\长度和容积测量-V2.0\长度和容积测量` |
| DLL 工程 | `LengthVolumeMeasureDll.vcxproj`（v142） |
| 打包脚本 | `V2.0/长度和容积测量-V2.0/长度和容积测量/scripts/package_sdk_to_ipc.ps1` |
| IPC SDK | `third_party/length_volume_measure/` |
| CMake | `cmake/LengthVolumeMeasureSdk.cmake` |
| 封装头文件 | `modules/length_volume_measure/include/scan_tracking/length_volume_measure/` |
| 封装实现 | `modules/length_volume_measure/src/length_volume_measure_service.cpp` |

### 8.4 依赖

| 依赖 | 说明 |
|------|------|
| PCL 1.12（vc142） | **仅 DLL 内部**；运行时随 SDK 部署 `pcl_*.dll`（含 `pcl_features`） |
| Eigen / Boost / FLANN | **仅 DLL 内部**；工位工程不依赖 |
| 轻量 PCD 加载 | DLL 内 `SimplePcdIo`，不依赖 `pcl_io`/VTK/OpenNI |
| 上游数据 | **内表面测量** 提供容积半径（直径/2）；本算法自算长度 |

工具链：算法 DLL 与 IPC 均使用 **MSVC v142（14.29）x64**。IPC Debug 工程可链接 **Release** 算法库（与焊缝等同策略）。

### 8.5 输入 / 输出

**输入**

- 扫描点云：`float` 交错 `x,y,z,...`（单位 mm）；非有限值在 DLL 内跳过  
- `volumeRadiusMm`：容积计算半径 (mm)，通常为内表面直径 / 2  
- 模板：内存点云，或由 `config.ini` 中的 PCD 路径加载  

**输出**（`LengthVolumeMeasurement` / `lvm_result`）

| 字段 | 含义 |
|------|------|
| `lengthMm` | 筒体长度 (mm) |
| `volumeLiters` | 容积 (L) |
| `volumeRadiusMm` | 容积计算所用半径 (mm) |
| `fittedOuterRadiusMm` | 拟合外半径 (mm) |
| `cylinder*` / `end1` / `end2` | 圆柱与两端定位细节 |
| `icpFitnessScore` / `icpConverged` | ICP 信息 |
| `valid` | 测量是否有效 |

### 8.6 调用方式（供后续主流程使用；当前未接线）

#### 方式 A：C++ 封装（推荐）

```cpp
#include "scan_tracking/length_volume_measure/length_volume_measure_service.h"

using scan_tracking::length_volume_measure::LengthVolumeMeasureService;
using scan_tracking::length_volume_measure::LengthVolumeMeasurement;
using scan_tracking::length_volume_measure::LengthVolumeMeasureError;

LengthVolumeMeasureService service;
LengthVolumeMeasureError err;
if (!service.initializeFromIni(/* configPath */ QString(), &err)) {
    // 或 service.initialize(config, templateXyz, n, &err);
    return;
}

const double volumeRadiusMm = innerDiameterMm * 0.5;  // 来自内表面测量
LengthVolumeMeasurement result;
if (!service.measure(scanXyz, scanCount, volumeRadiusMm, &result, &err)) {
    return;
}
// result.lengthMm, result.volumeLiters
```

- 默认配置路径：`<applicationDir>/config/length_volume_measure/config.ini`
- 同一实例内调用已串行化
- 生产环境建议内存模板 `initialize(...)`，避免 INI 内相对 PCD 路径失效

#### 方式 B：C API

头文件：`third_party/length_volume_measure/include/length_volume_measure_c_api.h`

```c
lvm_context* ctx = NULL;
lvm_create_from_ini("D:/path/config.ini", &ctx);

lvm_result result;
char msg[512];
lvm_measure(ctx, scan_xyz, scan_n, volume_radius_mm, &result, msg, sizeof(msg));

lvm_destroy(ctx);
```

### 8.7 与 flow_control 的关系

path3 / `algorithm=length_volume` 已在 `station2_inspection.cpp` 接线到 `evaluateLengthVolumeInspection` → `LengthVolumeMeasureService`。  
与现场一致：臂×7；采集后按 LB `Rt_global` 变换再合并外表面点云，一次 `lvm_measure`。  
模板：`config/length_volume_measure/config.ini` + `Data/sample_cylinder.pcd`（POST_BUILD 部署）。

**无需再改 SDK 接入层**（C API 与 V1 兼容）。

### 8.8 更新算法版本

在算法仓 `V2.0/长度和容积测量-V2.0/长度和容积测量` 执行：

```powershell
msbuild LengthVolumeMeasureDll.vcxproj /p:Configuration=Release /p:Platform=x64
powershell -ExecutionPolicy Bypass -File .\scripts\package_sdk_to_ipc.ps1 -Configuration Release
```

然后在 IPC 仓重新编译 `scan-tracking` 即可。

### 8.9 已知限制

1. 当前 SDK **仅打包 Release**；Debug 配置下 CMake 回退使用 Release DLL（与焊缝/厚度一致）。  
2. `initializeFromIni` 依赖 INI 内模板 PCD 的有效路径；生产环境建议改用内存模板 `initialize(...)`。  
3. 参考配置中的相对模板路径（如 `./Data/...`）部署到 exe 旁后通常无效，需现场配置或改内存入口。  
4. path3 / `length_volume` 走 `evaluateLengthVolumeInspection`（臂×7）；模板 PCD 与 LB 拼接仍需现场确认。  
5. 与第 6 节内表面 DLL 为 **上下游关系**：容积半径通常来自内表面直径。

---

## 9. 吊装点云碰撞检测（SDK 已打包）

### 9.1 功能说明

天车吊装防碰：背景体素占据 → 当前帧前景运动点 → 到固定障碍 AABB 最小距离 → WARNING/DANGER/EMERGENCY 连续帧确认。

> **源码**：`第二工位测量源码/辅助吊装中的碰撞检测-V1.2`  
> **状态**：✅ DLL + SDK + CMake 已就绪；⏸ `modules/collision_monitor` 薄封装与 Livox 点云流 / PLC 未接  
> **设备**：规划对接 Livox Mid360（碰撞雷达），非 TF 测距

### 9.2 接入状态

| 项 | 状态 |
|----|------|
| 算法 DLL `CollisionDetection.dll` | ✅（v142 / x64 / `/MD`，Release） |
| C API `collision_detection_c_api.h` | ✅ |
| SDK 打包至 `third_party/collision_detection` | ✅ |
| CMake `CollisionDetectionSdk.cmake` | ✅ |
| 封装模块 `scan_tracking_collision_monitor` | ⏸ 待建 |
| Livox 实时点云 / PLC 联调 | ⏸ 待后续 |

### 9.3 C API 调用顺序

```text
cd_create_from_ini(collision.ini)
cd_set_background(xyz)          # 无吊装物背景，只一次
loop:
  cd_detect(xyz, &result)       # 上下文内保留连续帧状态
  use result.confirmed_level / min_distance_m
cd_destroy
```

输入：交错 `float x,y,z`，**单位米**，与障碍/ROI 同一坐标系。

### 9.4 更新算法版本

```powershell
msbuild CollisionDetectionDll.vcxproj /p:Configuration=Release /p:Platform=x64
powershell -ExecutionPolicy Bypass -File .\scripts\package_sdk_to_ipc.ps1 -Configuration Release
```

---

## 10. 算法依赖关系（目标架构）

```text
                    ┌─────────────────────┐
                    │  ScanSegmentCache   │
                    │  (Mech-Eye 点云等)   │
                    └──────────┬──────────┘
                               │
         ┌─────────────────────┼─────────────────────┐
         │                     │                     │
         ▼                     ▼                     ▼
┌────────────────┐   ┌────────────────┐   ┌────────────────────┐
│ 焊缝截面测量 ✅ │   │ 厚度测量 ✅*    │   │ 内表面测量 ✅*      │
│ WeldMeasure    │   │ ThicknessMeas. │   │ InnerSurfaceMeas.  │
└───────┬────────┘   └────────────────┘   └─────────┬──────────┘
        │                                            │
        ▼                                            ▼
┌────────────────┐                        ┌────────────────────┐
│ 咬边长度 ✅*    │                        │ 长度和容积 ✅*      │
│ UndercutLength │                        │ LengthVolumeMeas.  │
└────────────────┘                        └────────────────────┘

并行安全链（独立传感器）：
  Livox Mid360 点云 ──► CollisionDetection.dll（吊装防碰）⏸

* 厚度 / 内表面 / 咬边长度 / 长度容积：工程已接入，主流程尚未调用
```

---

## 11. 变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-07-16 | v1.0 | 初版：焊缝截面测量 DLL 接入完成；其余四算法占位 |
| 2026-07-17 | v1.1 | 厚度测量 DLL/SDK/薄封装接入（Release）；主流程暂不调用 |
| 2026-07-17 | v1.2 | 内表面测量 DLL/SDK/薄封装接入（Release）；主流程暂不调用 |
| 2026-07-17 | v1.3 | 咬边长度测量 DLL/SDK/薄封装接入（Release）；主流程暂不调用 |
| 2026-07-17 | v1.4 | 长度和容积测量 DLL/SDK/薄封装接入（Release）；主流程暂不调用 |
| 2026-07-22 | v1.5 | 长度容积升级为 V2.0 SDK（outsideScan 原始点云分位数端点；`refineEdgePercentile=0.0001`） |
| 2026-07-23 | v1.6 | 长度容积更新为 V2.0 第二版 SDK（源码路径 `V2.0/长度和容积测量-V2.0`；C API 兼容；保留 `outlierK<=1` 跳过 SOR） |
| 2026-07-23 | v1.7 | 焊缝截面升级为 V2.0 SDK（源码路径 `V2.0/焊缝测量-VS2022-V2.0`；C API 兼容；咬边默认参数对齐 V2） |
| 2026-08-03 | v1.8 | 吊装点云碰撞检测：DLL/C API/SDK/`CollisionDetectionSdk.cmake` 就绪；薄封装与主流程未接 |