# IPC_Station2 工作交接说明

> **更新日期**：2026-09-18  
> **仓库**：第二工位 IPC 控制台（`stationId=2`）  
> **GitHub**：https://github.com/icon-handsome/LY_Station2  
> **Gitee**：https://gitee.com/wang-yixiao0205/ly_-station2  
> **读者**：接手同事 / 现场联调 / AI Agent

本文是**交接总览**。细节以 `docs/` 下专题文档为准，冲突时以代码与本页「当前状态」为准。

---

## 1. 项目是什么

兰铀**第二工位**多模式检测工控机程序：与 PLC（Modbus）握手，按扫描路径驱动 Mech-Eye / 海康 CXP / 海康智能相机等采集，段缓存落盘后做综合检测，结果写回 PLC 并推送 HMI（TCP）。

- **程序名**：`scan-tracking.exe`
- **默认工作模式**：圆筒半成品 `MODE_CYLINDER_SEMI`
- **第一工位**：已从本仓剥离，不要再按 `stationId=1` 维护

核心链路：

```text
PLC Trig_* → StateMachine → Handler
  → 采集（VisionPipeline / Mech-Eye / 海康…）→ ScanSegmentCache + output/run_*
  → Trig_Inspection → station2_inspection（按 path.algorithm 路由算法）
  → 写 PLC Res / NG + HMI event.inspection.*
```

---

## 2. 当前开发进度（截至 2026-09）

### 2.1 已完成

| 能力 | 状态 | 说明 |
|------|------|------|
| 第二工位骨架 + flow_control 重构 | ✅ | `PlcTaskHost` + `handlers/*`，见 `docs/guides/flow_control架构说明.md` |
| 外部/伸缩杆分段扫描 | ✅ | `Trig_ScanSegment`(40023) / `Trig_TelescopicScan`(40046) 分离 |
| 扫描路径 JSON + 运行时切路 | ✅ | `ScanPathId(40047)`；`activePathId` / 封头类型过滤 |
| 段缓存 + run 落盘 | ✅ | `output/run_{taskId}_*/path_*/arm|telescopic/{pointIndex}/` |
| path3 筒体总长 | ✅ | OOP worker，SDK V2.3；容积 IPC 按 πr²L 补算 |
| path4 厚度 + 内表面 | ✅ | 厚度默认 V3.2 worker；内表面 DLL；容积写入 Inspection |
| path2 / path6 编号 OCR | ✅ | 海康 C `NumberRecognition`（`Trig_CodeRead` / Inspection） |
| path1 / path5 焊缝截面路由 | ✅ | SDK V2.0 已接入；**逐段后台解算**已实现 |
| 吊装辅助（双 TF） | ✅ 基本可用 | `cmd.start_hoist_assist` 触发；成败暂主要看双 TF |
| 封头类型 | ✅ | HMI `cmd.set_head_type` / PLC 40186；单封头跑 6→5，无封头跑 1–4 |
| HMI TCP | ✅ | 端口默认 9900 |

### 2.2 重要运行开关（接手必看）

根目录 `config.ini`：

```ini
[FlowControl]
algorithmEnabled=false   ; 当前现场/本机默认：跳过算法，检测直接 OK，只跑采集
```

- **`algorithmEnabled=false`**：联调防崩溃用；Inspection 假成功放行。要跑真实算法务必改为 **`true`** 并重启。
- **`[Station] enableHoistAssist=false`**：普通扫描模式；吊装联调改为 `true`。
- **`hikCxpBypassOk=true`**：CXP 失败不阻断主流程（CXP 另用或未就绪时）。

### 2.3 待办 / 风险点

| 项 | 说明 |
|----|------|
| 焊缝截面输入形态 | 算法期望**截面点云**；整段 Mech 云直接喂入仍有切片/聚合策略待生产确认 |
| 咬边长度 | SDK 已接，`modules/undercut_length_measure` **主流程未调用** |
| 碰撞检测 | `third_party/collision_detection` SDK 就绪；Livox 流 + 薄封装 + PLC **未完整联调** |
| 吊装辅助 | 碰撞/第三路海康多为监视字段；成败门禁以双 TF 为主 |
| 内部扫描扩展 | 伸缩杆内部纵/环缝与滚轮协同（时序图 2.4）硬件就绪后继续 |
| 本地未提交改动 | 工作区尚有 `state_machine*`、`config_manager`、Mech-Eye、CMake、`scan_tracking_dev.cmd` 等修改，**交接前请确认是否 commit/push** |

旧版进度文 `docs/guides/双工位改造进度与续接指南.md`（2026-06）偏旧；算法细节以 `docs/算法接入与调用.md`（2026-09-16）为准。

---

## 3. 目录结构（该看哪里）

| 路径 | 用途 |
|------|------|
| `app/` | 主程序入口、CMake 部署规则 |
| `common/` | `ConfigManager`、路径/落盘工具 |
| `modules/flow_control/` | **状态机 + Handler + Inspection 路由（业务中枢）** |
| `modules/vision/` | 组合采集流水线 |
| `modules/mech_eye/` `modbus/` `hmi_server/` | 相机 / PLC / 显控 |
| `modules/weld_measure/` 等 | 各算法薄封装（不编算法源码） |
| `modules/hoist_assist/` `tfmini_plus/` `livox_mid360/` | 吊装 / 测距 / 雷达 |
| `config/` | 运行配置、scan_paths、各算法 ini/模板 |
| `config.ini` | **总入口配置（工位、相机 IP、算法开关）** |
| `third_party/` | SDK / DLL / OpenCV / Mech-Eye 等（**多数不进 Git 或体积大**） |
| `docs/` | 专题文档索引见 `docs/README.md` |
| `tools/scan_tracking_dev.cmd` | 一键 configure / build / run |
| `build/` `.vs/` | 本地编译与 IDE 缓存，**可删，勿交接** |

---

## 4. 扫描路径 / 路径点在哪改

### 4.1 一句话

**改点位数、采集相机组合、算法类型 → 只改 JSON，一般不用改 C++。**

主文件：

```text
config/scan_paths/station2_cylinder_semi.json
```

由 `config.ini` / profile 指定：

```ini
[Station]
scanPathsConfigPath=config/scan_paths/station2_cylinder_semi.json
profileIni=config/station_profiles/station2_cylinder_semi.ini
```

加载与校验：`common/src/config_manager.cpp`（`validateScanPathsConfig`、`findScanPointByIndex`）。

详细 schema：`docs/guides/扫描路径配置说明.md`。

### 4.2 当前路径一览（JSON v1.9）

| pathId | name | algorithm | 设备配额 | 备注 |
|--------|------|-----------|----------|------|
| 1 | straight_weld | weld_section | 臂×12 + 伸缩×7 | 无封头直焊缝 |
| 2 | code_read | code_read | 臂×1 | 无封头编号 |
| 3 | length_volume | length_volume | 臂×8 | 筒体总长；`volumeRadiusMm` |
| 4 | thickness_inner_surface | thickness_inner_surface | 臂×12 | 点 purpose 配对厚度/内表面 |
| 6 | endcap_code_read | code_read | 臂×1 | 单封头编号（JSON 排在 5 前） |
| 5 | ring_weld | weld_section | 臂×18 + 伸缩×18 | 单封头环缝 |
| 7 | self_check | self_check | 臂×2 | `enabled:false`，自检用 |

- **`activePathId`**：当前活跃路径；改后**重启 IPC**（或由 PLC 写 `40047 ScanPathId` 运行时切换）。
- **封头类型**：单封头有效顺序 `6 → 5`；无封头 `1 → 2 → 3 → 4`。
- **path4 点用途**：在 `points[].purpose` 改（`thickness_inner` / `thickness_outer` / `inner_surface`），不要只改点数不改 purpose。

### 4.3 常见修改场景

| 你想… | 改这里 |
|-------|--------|
| 某路径机械臂/伸缩杆点数 | JSON 对应 path 的 `devices[].totalPoints`；有 `points` 时保持数量一致 |
| 某点采哪些相机 | 该 path 的 `capture.arm` / `capture.telescopic`（`mechEye3d` / `hikSmartC` / `hikCxp`） |
| 切换检测算法类型 | `algorithm` 字段（须有 Inspection 路由支持） |
| 切换当前路径（静态） | JSON `activePathId` 后重启 |
| 切换当前路径（运行时） | PLC 写 `40047`（推荐）；旧逻辑：扫齐后再发段号 1 等兼容行为仍可能存在 |
| 相机 IP / 串号 | 根目录 `config.ini` `[Vision]` / `[Camera]` |
| 段扫失败清缓存策略 | `config.ini [FlowControl] scanFailurePolicy`（`segment`/`path`/`workpiece`） |

### 4.4 触发器与段号寄存器（勿混用）

| 执行机构 | PLC 触发 | 段号寄存器 | segmentKind |
|----------|----------|------------|-------------|
| 扫描机械臂 | `Trig_ScanSegment` | AO47 / 40015 | `external` |
| 伸缩杆 | `Trig_TelescopicScan` | AO48 / 40016 | `internal_*` |

Handler：

- `modules/flow_control/src/handlers/scan_segment_handler.cpp`
- `modules/flow_control/src/handlers/telescopic_scan_handler.cpp`
- 采集公共逻辑：`scan_capture_common.cpp`

---

## 5. 算法与 Inspection 在哪改

| 路径算法 | 封装模块 | SDK（third_party） | 主流程入口 |
|----------|----------|-------------------|------------|
| weld_section | `modules/weld_measure` | `weld_measure` | `station2_inspection.cpp` → `evaluateWeldSection*` |
| length_volume | `modules/container_total_length_measure` | `container_total_length_v2_3` | `evaluateLengthVolumeInspection` |
| thickness_inner_surface | `modules/thickness_measure_v2` + `inner_surface_measure` | `thickness_measure_v3_2` + `inner_surface_measure` | `evaluateThicknessInnerSurfaceInspection` |
| code_read | vision / 海康 C | MVS 等 | `code_read_handler.cpp` + Inspection |
| undercut（未接线） | `modules/undercut_length_measure` | `undercut_length_measure` | — |
| 碰撞（未接线） | `modules/collision_monitor`（待完善） | `collision_detection` | — |

- **总路由**：`modules/flow_control/src/station2_inspection.cpp`
- **Inspection Handler**：`handlers/inspection_handler.cpp`
- **算法专题**：`docs/算法接入与调用.md`
- **算法源码**（独立仓，不编进 IPC）：通常在 `D:\work\LY\第二工位测量源码\`，DLL 打包进本仓 `third_party/`

更新算法：在算法仓编 DLL → `package_*_sdk.ps1` → IPC 重新 `build`。C API 兼容时薄封装可不改。

---

## 6. 配置与联调速查

### 6.1 构建 / 运行

```powershell
cmd /c tools\scan_tracking_dev.cmd configure-debug
cmd /c tools\scan_tracking_dev.cmd build-debug
cmd /c tools\scan_tracking_dev.cmd run-debug
```

预设：`win-msvc2019-qtcore-ninja-debug` / `release`（见 `CMakePresets.json`）。  
工具链：**MSVC v142（14.29）x64** + Qt（与工程一致）。

### 6.2 网络与端口

| 服务 | 默认 |
|------|------|
| Modbus | `0.0.0.0:501` unitId=1 |
| HMI TCP | `9900` |
| 日志 | `logs/scan_tracking_*.txt`、`logs/algorithm_scan_tracking_*.txt` |

### 6.3 现场改 IP 时优先检查

- Mech 臂：`mechEyeArmKey`；伸缩：`mechEyeTelescopicKey`
- 海康 C 臂/伸缩 FTP 目录与 IP
- CXP 串号 `hikCxpCameraA/B*`
- Livox：`bin/mid360_config.json` 的 host/lidar IP
- TF：`[TfminiPlus]` COM 口

### 6.4 交接交付建议（体积）

本地目录常 **50GB+**，**不要整盘拷贝**。

| 内容 | 建议 |
|------|------|
| 源码 | 给 GitHub/Gitee 权限；`git clone`（`.git` 约数百 MB） |
| 算法 SDK / 相机 SDK | 单独打包 `third_party` 必要子集 + 模型/模板 |
| 大点云 Data | `config/weld_measure/Data` 等按需网盘交付 |
| 可删 | `build/`、`.vs/`、`logs/`、`_xfer/`、`_deploy_*`、临时 `_tmp_*` |

---

## 7. 关键代码索引

| 场景 | 路径 |
|------|------|
| 状态机入口 | `modules/flow_control/src/state_machine.cpp` |
| Modbus 轮询/触发 | `state_machine_modbus.cpp` |
| 段扫完成/落盘回调 | `state_machine_scan.cpp` |
| Inspection 写 PLC | `state_machine_inspection.cpp` |
| Handler 注册 | `handlers/task_handler_registry.cpp` |
| 段缓存 | `scan_segment_cache.cpp` |
| run 目录规则 | `common/src/capture_cache_paths.cpp` |
| 配置总管 | `common/src/config_manager.cpp` |
| 吊装辅助 | `modules/hoist_assist/src/hoist_assist_service.cpp` |
| 协议 | `docs/protocols/` |
| HMI 交接 | `docs/hmi/HMI开发交接说明.md` |
| 时序图 2.1～2.5 | `docs/sequence_diagrams/` |

新增 PLC 触发器：只加 `handlers/` + 注册表；能力不足再扩 `PlcTaskHost`，**不要** `friend` 进 `StateMachine` private。

---

## 8. 接手第一周建议清单

1. `git clone` + 拿到 `third_party` / 模型包，能 `build-debug` 通过  
2. 对照现场改 `config.ini` 相机与网口；先保持 `algorithmEnabled=false` 跑通段扫落盘  
3. 熟悉 `station2_cylinder_semi.json` 与 PLC `40047` 切路  
4. 打开 `algorithmEnabled=true`，逐路径（1→4 或 6→5）验 Inspection / HMI 指标  
5. 阅读 `docs/算法接入与调用.md` + `docs/guides/扫描路径配置说明.md`  
6. 确认未提交改动是否入库，再删本机 `build/` 瘦身  

---

## 9. 文档地图

| 文档 | 用途 |
|------|------|
| [docs/README.md](docs/README.md) | 文档总索引 |
| [docs/算法接入与调用.md](docs/算法接入与调用.md) | 算法 DLL 版本与调用（最新） |
| [docs/guides/扫描路径配置说明.md](docs/guides/扫描路径配置说明.md) | JSON / Res 码 / 落盘 |
| [docs/guides/flow_control架构说明.md](docs/guides/flow_control架构说明.md) | Handler 架构 |
| [docs/guides/双工位改造进度与续接指南.md](docs/guides/双工位改造进度与续接指南.md) | 早期进度（部分过时） |
| [docs/hmi/HMI开发交接说明.md](docs/hmi/HMI开发交接说明.md) | 显控 TCP |
| [README.md](README.md) | 仓库简介与构建入口 |

---

*本交接说明由当前仓库状态整理；现场参数以工控机上实际 `config.ini` 为准。*
