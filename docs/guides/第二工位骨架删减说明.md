# 第二工位 IPC 骨架删减说明

> **文档版本**：v1.3  
> **更新日期**：2026-07-20  
> **仓库**：`IPC_Station2`（第二工位专用）  
> **读者**：IPC 开发 / 接手 AI Agent / HMI 联调

---

## 1. 目标与结论

本仓库为 **第二工位专用 IPC**：保留相机采集、Modbus 握手、HMI TCP、PLC Handler；已剥离第一工位坡口/开孔/LBN 等算法。**2026-07 起已回迁 LB 光学双目位姿跟踪**（不含 LBN）：机械臂扫描并行 Mech + CXP + 海康 C，LB 解算后变换 Mech 点云再落盘。

| 类别 | 状态 |
|------|------|
| 默认工位 | `stationId=2`，profile `station2_cylinder_semi.ini` |
| `Trig_ScanSegment` | **已实现**：scan_paths + 组合采集 + LB（臂段）+ 段缓存落盘 |
| `Trig_PoseCheck` | **已实现**：离线 BMP → `runLegacyLbPoseCheck` |
| `Trig_Inspection` | **已实现**：段缓存校验 + 测量算法路由 |
| HMI TCP | **完整保留** |
| 构建依赖 | Qt + Mech-Eye + Orbbec + Livox + HIK MVS + **OpenCV 3.4.3（LB）**；点云变换用 Eigen（无系统 PCL） |

---

## 2. 已删除内容（相对第一工位）

### 2.1 算法与模块

| 路径 | 说明 |
|------|------|
| `third_party/Po_Kou_Ce_Liang` | 坡口测量 |
| `third_party/LBN` | LBN 转盘位姿（**仍剔除**） |
| `third_party/Hole`、`third_party/Thicknessmeasurement` | 开孔 / 壁厚（旧） |
| 完整 `TrackingService` 检测编排 | 坡口/开孔/厚度编排；现仅保留瘦身 `lb_pose_check` |
| `cmake/PclRuntime.cmake` | 系统 PCL 部署（**仍剔除**；LB 拼接用 Eigen） |

### 2.1.1 已回迁（相对 v1.2 删减）

| 路径 | 说明 |
|------|------|
| `third_party/LB` | LB 静态库 + S2 `track_config.ini` / 扫描仪模板 |
| `cmake/OpenCvBundle.cmake` | OpenCV 3.4.3 查找与运行时部署 |
| `modules/vision/lb_pose_detection_adapter.*` | 在线 CXP → LB |
| `modules/tracking/lb_pose_check.*` | `Trig_PoseCheck` 离线校验 |
| `modules/mech_eye/point_cloud_processor.*` | Eigen 实现 `transformPointCloudFrame` |
| `config.ini [LbPose]`、`hikCxpEnabled=true` | LB / CXP 开关 |

### 2.2 状态机与流程

- 第一工位 `state_machine.cpp`（约 3900 行）已裁剪并**按职责拆分为多文件**（见 [flow_control架构说明](./flow_control架构说明.md)）
- 移除：多路径点云缓存、**LBN** 标定链、后台 PCL refinement、`executeBypassActiveTask`、`algorithmBypassEnabled`
- **保留并接入**：`setInspectionResultPublisher`、`ScanSegmentCache`、`station2_inspection`；臂段 LB 拼接在 `state_machine_scan.cpp`
### 2.3 配置与文档

- 删除 `config/station_profiles/station1_endcap.ini`、`config/scan_paths/station1_default.json`
- 三个重复的 `station2_*.json` 合并为 **`config/scan_paths/station2_placeholder.json`**
- 删除 `docs/station1/`、`docs/sequence_diagrams/station1/` 及根目录 `scan_paths_config.json`

---

## 3. 保留内容

### 3.1 相机与设备

- Mech-Eye、海康 CXP/Mono/C、`VisionPipelineService`（纯采集）
- Orbbec Gemini、Livox MID-360、Tfmini Plus
- Modbus 轮询 / 握手 / 超时 / 故障字

### 3.2 流程控制

9 个 Handler 均已注册（`modules/flow_control/src/handlers/`），业务在 Handler 内实现，通过 **`PlcTaskHost`**（`ctx.host`）调用 PLC/状态能力：

| 触发器 | 当前行为 |
|--------|----------|
| `Trig_ScanSegment` | Handler 发起组合采集；StateMachine 异步落盘；Res=1/5/6/7 |
| `Trig_Inspection` | Handler 调用 `evaluateInspectionForActiveTask` + `finishInspection` |
| `Trig_LoadGrasp` 等 | 各 Handler 占位实现（自检、位姿、编号等） |

架构详见 [flow_control架构说明.md](./flow_control架构说明.md)。

### 3.3 HMI（勿删）

- 模块：`modules/hmi_server/`
- 推送：`publishInspectionResult`、`publishInitialInspectionDisplay`、`status.*`、`event.scan.*` 等
- 协议占位（兼容旧显控）：
  - `cmd.set_bevel_recipe` → 返回「第二工位未实现」
  - `cmd.debug_trigger_inspection` → 从段缓存评估并 `publishInspectionResult`（**不写 PLC**）

第二工位检测实现后，在业务层调用：

```cpp
hmiTcpServer->publishInspectionResult(inspectionResult);
```

---

## 4. 默认配置

```ini
# config.ini
[Station]
stationId=2
profileIni=config/station_profiles/station2_cylinder_semi.ini
scanPathsConfigPath=config/scan_paths/station2_cylinder_semi.json
defaultWorkMode=MODE_CYLINDER_SEMI

[FlowControl]
pollIntervalMs=500
heartbeatIntervalMs=1000
simulatedProcessingMs=300
```

三种 profile 默认仍指向 `station2_placeholder.json`（封头/成品联调）；圆筒模式使用 **`station2_cylinder_semi.json`**（3 外部点）。详见 [扫描路径配置说明](./扫描路径配置说明.md)。

### 4.1 `station2_placeholder.json`

单段占位路径，供 Modbus 日志与 HMI `cmd.capture_bundle` 最小联调：

```json
{
  "version": "1.0",
  "scanPaths": [
    {
      "pathId": 1,
      "enabled": true,
      "totalPoints": 1,
      "points": [{ "pointIndex": 1, "needRotation": false }]
    }
  ]
}
```

### 4.2 `station2_cylinder_semi.json`

圆筒半成品外部扫描 3 点位（`segmentKind=external`）；内部伸缩杆段待硬件后追加。

---

## 5. 构建与运行

```bat
cmd /c tools\scan_tracking_dev.cmd configure-debug
cmd /c tools\scan_tracking_dev.cmd build-debug
cmd /c tools\scan_tracking_dev.cmd run-debug
```

- CMake 预设：`win-msvc2019-qtcore-ninja-debug` / `release`
- `CMakePresets.json` 已去掉 PCL/OpenCV/算法开关；Mech-Eye SDK 指向本仓库 `third_party`
- `tools/scan_tracking_dev.cmd`、`.vscode/launch.json` 的 PATH 已去掉 PCL/VTK/OpenNI2/OpenCV

---

## 6. 续接开发（下一步）

1. 阅读 [`扫描路径配置说明.md`](./扫描路径配置说明.md) 与 [`工作流程-AI-Agent参考.md`](./工作流程-AI-Agent参考.md)
2. 接入缺陷/编号/3D 算法（替换 `station2_inspection.cpp` 占位逻辑）
3. 按工位模式扩展内部伸缩杆段（`segmentKind=internal_*`）

详见 [`双工位改造进度与续接指南.md`](./双工位改造进度与续接指南.md)。

---

## 7. 相关文档

| 文档 | 用途 |
|------|------|
| [flow_control架构说明](./flow_control架构说明.md) | StateMachine / PlcTaskHost / Handler 分工 |
| [双工位改造进度与续接指南](./双工位改造进度与续接指南.md) | 阶段规划与硬件清单 |
| [HMI开发交接说明](../hmi/HMI开发交接说明.md) | TCP 协议与 Server 能力 |
| [封头检测工位_TCP_IP显控通信协议_v1.0](../protocols/封头检测工位_TCP_IP显控通信协议_v1.0.md) | 消息与 payload 字段 |

---

*最后更新：2026-06-16*
